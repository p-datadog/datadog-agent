load("@rules_go//go:def.bzl", "go_library")
load("//bazel/rules/dd_go_test:defs.bzl", "dd_go_test")

go_library(
    name = "payload",
    srcs = [
        "pathevent.go",
        "traceroute_errors.go",
        "utils.go",
    ],
    importpath = "github.com/DataDog/datadog-agent/pkg/networkpath/payload",
    visibility = ["//visibility:public"],
    # gazelle keeps reverting this to @com_github_datadog_datadog_agent_pkg_network_payload
    deps = ["//pkg/network/payload"],  # keep
)

dd_go_test(
    name = "payload_test",
    srcs = ["payload_test.go"],
    embed = [":payload"],
    deps = ["@com_github_stretchr_testify//require"],
)
